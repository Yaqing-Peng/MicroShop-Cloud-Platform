spring:
  application:
    name: userservice
  profiles:
    active: dev # environment
  cloud:
    nacos:
      server-addr: localhost:8848 # nacos addr
      config:
        file-extension: yaml # file format